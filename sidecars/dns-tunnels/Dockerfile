# Placeholder — Phase 5 will bundle masterdns and dnstt binaries.
FROM alpine:3.20
RUN echo "dns-tunnels sidecar placeholder" && exit 0
CMD ["sh", "-c", "echo 'dns-tunnels not yet configured' && sleep infinity"]
